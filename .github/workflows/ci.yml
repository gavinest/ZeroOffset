name: ci

on:
  push:
  pull_request:
    branches:
      - main

jobs:
  validate: 
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: lint
        run: make lint

# deploy:
#   runs-on: ubuntu-latest
#   needs: validate
#   if: github.ref == 'refs/heads/main'
#   steps:
#     - name: checkout
#       uses: actions/checkout@v3

#     - name: deploy
#       run: make deploy
